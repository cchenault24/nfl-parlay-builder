{
  "name": "nfl-parlay-builder-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "prebuild": "yarn workspace @npb/shared run build",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@npb/shared": "*",
    "cors": "^2.8.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
